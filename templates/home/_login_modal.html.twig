<div class="fixed z-20 inset-0 overflow-y-auto hidden" aria-labelledby="login-modal" role="dialog" aria-modal="true" id="login-modal">
	<div class="flex items-start justify-center min-h-screen pt-32 px-4 pb-20 text-center">
		<div class="fixed inset-0 bg-gray-500 bg-opacity-75 transition-opacity" aria-hidden="true"></div>
		<!-- This element is to trick the browser into centering the modal contents. -->
		<span class="hidden sm:inline-block sm:align-middle" aria-hidden="true">&#8203;</span>
		<div class="inline-block align-bottom bg-white text-left overflow-hidden shadow-xl transform transition-all sm:my-8 sm:align-middle sm:max-w-lg sm:w-full">
			{% include "/login/_login_form.html.twig" %}
		</div>
	</div>
</div>
